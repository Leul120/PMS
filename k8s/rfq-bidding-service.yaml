apiVersion: apps/v1
kind: Deployment
metadata:
  name: rfq-bidding-service
  namespace: procurement
  labels:
    app: rfq-bidding-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rfq-bidding-service
  template:
    metadata:
      labels:
        app: rfq-bidding-service
    spec:
      containers:
        - name: rfq-bidding-service
          image: procurement/rfq-bidding-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres-rfq:5432/rfqdb"
            - name: SPRING_DATA_REDIS_HOST
              value: "redis-rfq"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: rfq-db-password
            - name: JWT_PUBLIC_KEY_PATH
              value: "file:/etc/jwt-keys/jwt-public.key"
          volumeMounts:
            - name: jwt-keys
              mountPath: /etc/jwt-keys
              readOnly: true
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: jwt-keys
          secret:
            secretName: jwt-keys

---
apiVersion: v1
kind: Service
metadata:
  name: rfq-bidding-service
  namespace: procurement
spec:
  selector:
    app: rfq-bidding-service
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP
