apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-invoice-service
  namespace: procurement
  labels:
    app: delivery-invoice-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: delivery-invoice-service
  template:
    metadata:
      labels:
        app: delivery-invoice-service
    spec:
      containers:
        - name: delivery-invoice-service
          image: procurement/delivery-invoice-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres-delivery:5432/deliverydb"
            - name: SPRING_DATA_REDIS_HOST
              value: "redis-delivery"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: delivery-db-password
            - name: JWT_PUBLIC_KEY_PATH
              value: "file:/etc/jwt-keys/jwt-public.key"
          volumeMounts:
            - name: jwt-keys
              mountPath: /etc/jwt-keys
              readOnly: true
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: jwt-keys
          secret:
            secretName: jwt-keys

---
apiVersion: v1
kind: Service
metadata:
  name: delivery-invoice-service
  namespace: procurement
spec:
  selector:
    app: delivery-invoice-service
  ports:
    - port: 8085
      targetPort: 8085
  type: ClusterIP
